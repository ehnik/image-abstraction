This API returns Google image search result data for a given parameter.

Parameters:
1) query (required)
2) offset (optional): defines number of results returned (max 10)

Example: http://localhost:3000/tigers?offset=4
